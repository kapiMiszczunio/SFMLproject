#pragma once
/*
* enum responsible for game difficulty
*/
enum difficulty {
	easy = 1,
	medium = 2,
	hard = 3
};

/*
* enum responsible for user choice
*/
enum continuation {
	tryAgain = 1,
	end = 2
};